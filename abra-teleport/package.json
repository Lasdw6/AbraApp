{
  "name": "@abra/teleport",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "bin": {
    "abra-teleport": "build/bin/abra-teleport.js"
  },
  "scripts": {
    "build": "tsx ../scripts/build-cli.ts",
    "check": "tsc --noEmit && tsc -p tsconfig.test.json",
    "test": "npm run build && tsx --test test/*.test.ts",
    "package:agent": "bash scripts/package-agent.sh"
  },
  "engines": {
    "node": ">=22"
  },
  "files": [
    "build"
  ]
}
